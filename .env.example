# Dunbar clinic — environment template.
# Copy to .env for local development and fill in real values.
# NEVER commit the real .env file.

# Flask secret key (dev only; use a strong random value in production)
SECRET_KEY=change-me-to-a-long-random-string

# Path to the SQLite database file (local, offline)
DATABASE_PATH=data/clinic.db
